name: Tests

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.9", "3.10", "3.11"]
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}
        cache: 'pip'
    
    - name: Install dependencies
      working-directory: ./advanced_rag
      timeout-minutes: 20
      run: |
        python -m pip install --upgrade pip
        # Install minimal test dependencies only (unit tests don't need ML deps)
        pip install -r requirements-test.txt
      env:
        PIP_DEFAULT_TIMEOUT: 300
    
    - name: Run unit tests
      working-directory: ./advanced_rag
      timeout-minutes: 10
      run: |
        pytest tests/unit -m unit -v --tb=short
      env:
        OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY || 'test-key-for-ci' }}
        GROQ_API_KEY: ${{ secrets.GROQ_API_KEY || 'test-key-for-ci' }}
    
    - name: Check code style (if flake8 available)
      working-directory: ./advanced_rag
      run: |
        pip install flake8 || true
        flake8 src --count --select=E9,F63,F7,F82 --show-source --statistics || true
        flake8 src --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics || true

  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: "3.11"
    
    - name: Install dependencies
      working-directory: ./advanced_rag
      timeout-minutes: 20
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements-test.txt
        pip install langchain-core langchain-openai || true
      env:
        PIP_DEFAULT_TIMEOUT: 300
    
    - name: Check imports
      working-directory: ./advanced_rag
      run: |
        python -c "import sys; sys.path.insert(0, 'src'); from src.core import Result, get_config; print('✅ Imports OK')"

